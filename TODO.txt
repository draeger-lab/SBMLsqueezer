TODO-Liste
==========

Edukt- und Produktlisten können leer sein. Dieser Spezialfall wird bisher
nicht in den einzelnen Kinetiken behandelt.

Hier wird an vielen Stellen von einer Stöchiometrie ausgegangen.
Es können aber auch komplexe Ausdrücke vorkommen. Diesen muss
Rechnung getragen werden.

Problem mit logischen UND, ODER etc.

In Hill-Kinetik dafür sorgen, dass nicht mit der Gen-Konzentration
multipliziert und der Parameter Ks nicht mit dem Hill-Koeffizienten
potenziert wird (sonst Probleme mit Einheiten).

Direkt am Vorschaufenster für kinetische Gleichungen könnte ein Knopf
sein, mit dem die Gleichung nach LaTeX oder Text gespeichert werden
könnte.

Bug erkannt im CellDesigner: Im Biomodell 5 verschwindet bei Anwendung
der Liebermeister-Kinetiken der Parameter R für die Gaskonstante.

Problem in jsbml: Änderungen an CVTerms können nicht richtig mit libSBML
synchronisiert werden.

Probleme mit UTF-8 in jsbml: libSBML-Parser wandeln alle Zeichen in
UTF-8 um. Beim Synchronisieren müssen Sonderzeichen wieder zu den Zeichen-
Codes übersetzt werden.


Probleme bei der Simulation von Test Suite Modellen
===================================================

Modelle 203 und 204 vom Aufbau her gleich, aber die Simulation von 204
liefert nur das richtige Ergebniss, wenn die Quadratwurzel des Compartiments verrechnet
wird anstelle nur der normale Wert des Compartiments.
Das gleiche gilt f�r 373 und 374

Modell 354 wirft einen Fehler aufgrund von Problemen bei der Umsetzung der Funktion add
im Event Assignment S2 = add(k3, k4) mit 
Id 	 Arguments 	 Formula
add 	x, y 	x + y

Modell 355, �hnliches Problem wie bei 354 diesmal beim Auswerten von Trigger lessthan(S1, 0.5) mit
Id 	 Arguments 	 Formula
lessthan 	x, y 	x < y

Modell 356 wirft einen Fehler aufgrund von Problemen bei der Umsetzung der Funktionen
im Event Assignment S2 = add(k3, k4) und S1 = 1 � divide(k3, k4) mit 
Id 	 Arguments 	 Formula
add 	x, y 	x + y
divide 	x, y 	xy

Modell 357, 358 und 359 haben die selben Probleme


 